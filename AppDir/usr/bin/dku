#!/bin/bash
case "$1" in
    bash)
        shift
        exec "$APPDIR/usr/bin/dku-bash" "$@"
        ;;
    sh)
        shift
        exec "$APPDIR/usr/bin/dku-sh" "$@"
        ;;
    *)
        echo "Usage: dku [bash|sh] [CONTAINER NAME/ID]"
        exit 1
        ;;
esac

